# Features

Use this folder for brand/domain-specific modules.

Examples:
- `features/visuales/*`
- `features/audio/*`

Rules:
- Keep feature business rules here.
- Do not place shared auth/utilities here; use `src/shared`.

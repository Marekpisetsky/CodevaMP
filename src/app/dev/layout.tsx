import "../../styles/domains/dev.css";
import "../../styles/domains/dev-studio.css";

export default function DevLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
